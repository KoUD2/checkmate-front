---
phase: 03-variant-composer
plan: 05
type: execute
wave: 4
depends_on:
  - 04
files_modified:
  - frontend/src/app/admin/variants/page.tsx
  - frontend/src/app/admin/variants/AdminVariants.module.css
  - frontend/src/app/admin/variants/new/page.tsx
  - frontend/src/app/admin/variants/VariantForm.tsx
  - frontend/src/app/admin/variants/VariantForm.module.css
autonomous: true
requirements:
  - VARIANT-01
  - VARIANT-03
tags:
  - nextjs
  - react
  - admin
  - variants
  - list-page
  - create-form

must_haves:
  truths:
    - "Navigating to /admin/variants renders a list of variants with title, task count, published status, created date, and Edit + publish-toggle actions"
    - "Empty state shows 'Вариантов ещё нет' heading + 'Создайте первый вариант и добавьте задания из банка.' body"
    - "Clicking '+ Создать вариант' navigates to /admin/variants/new"
    - "On /admin/variants/new, the form renders title (required) and description (optional) fields"
    - "Submitting a valid form POSTs to /admin/variants and redirects to /admin/variants/[newId] (the builder page from Plan 06)"
    - "Clicking the publish toggle on a list row optimistically flips the status, calls PATCH /admin/variants/:id with { published }, and reverts on API error with an inline 3-second error message"
    - "Pagination renders when totalPages > 1; arrows correctly increment/decrement page state and refetch"
    - "Cancel link on create form returns to /admin/variants"
  artifacts:
    - path: "frontend/src/app/admin/variants/page.tsx"
      provides: "AdminVariantsPage client component — table + pagination + publish-toggle UI"
      contains: "AdminVariantsPage"
    - path: "frontend/src/app/admin/variants/AdminVariants.module.css"
      provides: "CSS module reusing AdminTaskBank classes + new togglePublish / statusBadge classes"
      contains: ".togglePublish"
    - path: "frontend/src/app/admin/variants/new/page.tsx"
      provides: "NewVariantPage server component wrapping <VariantForm />"
      contains: "VariantForm"
    - path: "frontend/src/app/admin/variants/VariantForm.tsx"
      provides: "VariantForm client component — title + description fields, submit handler, error banner"
      contains: "export default function VariantForm"
    - path: "frontend/src/app/admin/variants/VariantForm.module.css"
      provides: "CSS module mirroring ExamTaskForm.module.css classes"
      contains: ".form"
  key_links:
    - from: "frontend/src/app/admin/variants/page.tsx"
      to: "frontend/src/services/variants.service.ts"
      via: "variantsService.adminList(page) + variantsService.adminUpdate(id, { published })"
      pattern: "variantsService\\."
    - from: "frontend/src/app/admin/variants/VariantForm.tsx"
      to: "frontend/src/services/variants.service.ts"
      via: "variantsService.adminCreate({ title, description })"
      pattern: "adminCreate"
    - from: "frontend/src/app/admin/variants/new/page.tsx"
      to: "frontend/src/app/admin/variants/VariantForm.tsx"
      via: "<VariantForm />"
      pattern: "VariantForm"
---

<objective>
Build the admin-side list page (`/admin/variants`) and create form (`/admin/variants/new`). These two surfaces deliver VARIANT-01 (admin can create a variant) and VARIANT-03 (admin can publish/unpublish), while the builder page from Plan 06 delivers VARIANT-02 task assignment. Plan 07 delivers VARIANT-04 / VARIANT-05 student surfaces. All three Wave-4 plans run in parallel — disjoint files, common dependency on Plan 04's variantsService.

Purpose: List page is the admin landing for variant management; create form is the entry into the builder. Both reuse Phase 2 styling primitives (AdminTaskBank.module.css for the list, ExamTaskForm.module.css for the form) per UI-SPEC §"Admin Variant Create Form" and §"Admin Variants List".
Output: 5 files. The directory `frontend/src/app/admin/variants/` is the canonical home; Plan 06 will add a `[id]/` subdirectory next to these files (no file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-variant-composer/03-CONTEXT.md
@.planning/phases/03-variant-composer/03-PATTERNS.md
@.planning/phases/03-variant-composer/03-UI-SPEC.md
@.planning/phases/03-variant-composer/03-04-PLAN.md
@frontend/src/services/variants.service.ts
@frontend/src/app/admin/task-bank/page.tsx
@frontend/src/app/admin/task-bank/AdminTaskBank.module.css
@frontend/src/app/admin/task-bank/new/page.tsx
@frontend/src/app/admin/task-bank/ExamTaskForm.tsx
@frontend/src/app/admin/task-bank/ExamTaskForm.module.css

<interfaces>
From `frontend/src/services/variants.service.ts` (shipped in Plan 04) — methods this plan calls:
- `variantsService.adminList(page = 1, limit = 20): Promise<VariantListResult>` where VariantListResult = `{ items: VariantListItem[], total, totalPages }`.
- `variantsService.adminUpdate(id: string, dto: Partial<CreateVariantPayload>): Promise<VariantListItem>` — used for publish toggle.
- `variantsService.adminCreate(dto: CreateVariantPayload): Promise<VariantListItem>` — used by create form.

`VariantListItem` shape (key fields needed by this plan):
- `id: string`, `title: string`, `description: string | null`, `published: boolean`, `createdAt: string`, `updatedAt: string`, `variantTasks: VariantTask[]`, `_count?: { variantTasks: number }`.

For the list page task count: prefer `item._count?.variantTasks ?? item.variantTasks.length ?? 0`. The backend `adminList` (Plan 02) includes `_count`, so `_count.variantTasks` will be present.

From `frontend/src/app/admin/task-bank/page.tsx` (analog list page):
- 'use client' directive at top
- State pattern: `items, page, totalPages, total, loading` declared with `useState`
- `fetchItems(overridePage?)` async function inside the component
- `useEffect(() => { fetchItems() }, [page, ...])` for refetch on page/filter change
- Header row with title + create button on the right
- Table with `<table className={styles.table} style={{ opacity: loading ? 0.6 : 1 }}>`
- Pagination block at the bottom (only when totalPages > 1)
- Empty state inside table tbody when items.length === 0

From `frontend/src/app/admin/task-bank/new/page.tsx` (analog new-page wrapper):
- Server component (no 'use client') — just a layout wrapper around the client form
- Back link + heading + `<VariantForm />` inside a flex column

From `frontend/src/app/admin/task-bank/ExamTaskForm.tsx` lines 50-90 (analog form component):
- `'use client'`
- `useRouter` from `next/navigation` for redirect after submit
- `useState` for form fields
- `handleSubmit` calls service.create then `router.push('/admin/variants/${result.id}')`
- Try/catch with `axiosError.response?.data?.message` fallback

CSS from `AdminTaskBank.module.css` to REUSE (do not redefine in AdminVariants.module.css): `.page`, `.header`, `.title`, `.btn_create`, `.filters`, `.input`, `.select`, `.table` (+ `th`, `td`), `.titleLink`, `.actions`, `.btn_edit`, `.btn_delete`, `.pagination`, `.empty`, `.emptyTitle`, `.emptySub`, `.sectionChip`. To "reuse", import directly: `import styles from '../task-bank/AdminTaskBank.module.css'` — same pattern as Phase 2 reuse.

ACTUALLY per Phase 2 SUMMARY of Plan 02-05, the convention is to copy CSS classes into the new module if they are heavily customized OR import the source module directly if they are reused unchanged. For Phase 3: create a new `AdminVariants.module.css` that contains ONLY the new classes (.togglePublish*, .statusBadge) and have the page component import BOTH:
```
import sharedStyles from '../task-bank/AdminTaskBank.module.css'
import styles from './AdminVariants.module.css'
```
Then use `sharedStyles.table` for the table and `styles.togglePublish` for the new bits. This avoids 200 lines of CSS duplication.

UI-SPEC copy strings to honor verbatim:
- Page title: `Варианты (N)` — N = total count
- Create CTA: `+ Создать вариант`
- Empty state heading: `Вариантов ещё нет`
- Empty state body: `Создайте первый вариант и добавьте задания из банка.`
- Publish toggle published label: `Опубликован`
- Publish toggle draft label: `Черновик`
- Publish toggle error (inline 3-sec): `Ошибка при публикации — попробуйте снова`
- Form: title field label, submit button `Создать вариант`, cancel link `← Варианты`, error banner `Не удалось создать вариант. Попробуйте ещё раз.`

UI-SPEC togglePublish CSS (from §"Admin Variants List"):
- Base class `.togglePublish`: `border: none; padding: 4px 12px; border-radius: 12px; font-size: 12px; font-weight: 600; cursor: pointer; transition: opacity 0.12s`
- Published modifier `.togglePublish_published`: `background: #c4edc8; color: #15803d`
- Draft modifier `.togglePublish_draft`: `background: #f3f4f6; color: #6b7280`
- Disabled `.togglePublish:disabled`: `opacity: 0.5; cursor: default`

Two-second error auto-dismiss: use `setTimeout(() => setRowError(null), 3000)` inside the catch block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminVariants.module.css + admin variants list page</name>
  <files>
    frontend/src/app/admin/variants/AdminVariants.module.css,
    frontend/src/app/admin/variants/page.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/task-bank/page.tsx (verbatim list-page structure — fetch, render, paginate)
    - frontend/src/app/admin/task-bank/AdminTaskBank.module.css (CSS classes to import as sharedStyles)
    - frontend/src/services/variants.service.ts (adminList, adminUpdate signatures)
    - .planning/phases/03-variant-composer/03-UI-SPEC.md §"Admin Variants List" §"Copywriting Contract"
  </read_first>
  <action>
    **`frontend/src/app/admin/variants/AdminVariants.module.css`** — create new file. Tab indentation. Contains ONLY new classes specific to the variants list (not present in AdminTaskBank.module.css):

    Add these classes verbatim from UI-SPEC §"Admin Variants List":
    - `.togglePublish` (base button) — `border: none; padding: 4px 12px; border-radius: 12px; font-size: 12px; font-weight: 600; cursor: pointer; transition: opacity 0.12s;`
    - `.togglePublish_published` — `background: #c4edc8; color: #15803d;`
    - `.togglePublish_draft` — `background: #f3f4f6; color: #6b7280;`
    - `.togglePublish:disabled` — `opacity: 0.5; cursor: default;`
    - `.statusBadge` — `display: inline-block; border-radius: 12px; padding: 4px 8px; font-size: 12px; font-weight: 600;`
    - `.rowError` — inline 3-second error text per UI-SPEC State Inventory: `display: inline; margin-left: 12px; font-size: 12px; color: #dc2626;`

    DO NOT redefine `.page`, `.header`, `.title`, `.table`, `.btn_create`, `.btn_edit`, `.pagination`, `.empty*` — those come from `AdminTaskBank.module.css` via the shared import.

    **`frontend/src/app/admin/variants/page.tsx`** — create new file. `'use client'` at top.

    Imports:
    ```
    import Link from 'next/link'
    import { useEffect, useState } from 'react'
    import variantsService, { VariantListItem } from '@/services/variants.service'
    import sharedStyles from '../task-bank/AdminTaskBank.module.css'
    import styles from './AdminVariants.module.css'
    ```

    Helper `formatDate(iso: string)` copied from `task-bank/page.tsx` lines 30-33 verbatim (returns `DD.MM.YYYY`).

    Component state:
    - `items: VariantListItem[]` (init [])
    - `page: number` (init 1)
    - `totalPages: number` (init 1)
    - `total: number` (init 0)
    - `loading: boolean` (init false)
    - `toggleBusyId: string | null` (init null) — tracks which row's publish toggle is in-flight
    - `rowError: { id: string; msg: string } | null` (init null) — last-failed row error

    `fetchItems(overridePage?: number)` async function:
    - Set loading true
    - Try `variantsService.adminList(overridePage ?? page)` → setItems, setTotal, setTotalPages from result
    - Catch → setItems([])
    - Finally → loading false

    `useEffect(() => { fetchItems() }, [page])` — refetch when page changes.

    `handleTogglePublish(item: VariantListItem)` async:
    - `setToggleBusyId(item.id)`, clear `rowError`
    - Optimistically update local items state: flip published in the row
    - `await variantsService.adminUpdate(item.id, { published: !item.published })`
    - On success: re-set the item from server response (canonicalize updatedAt)
    - On error: revert local state, set `rowError = { id: item.id, msg: 'Ошибка при публикации — попробуйте снова' }`, `setTimeout(() => setRowError(null), 3000)`
    - Finally: `setToggleBusyId(null)`

    Render structure:
    - Wrapper `<div className={sharedStyles.page}>`
    - Header `<div className={sharedStyles.header}>`: `<h1 className={sharedStyles.title}>Варианты ({total})</h1>` and `<Link href="/admin/variants/new" className={sharedStyles.btn_create}>+ Создать вариант</Link>`
    - Table with columns: `Название` (link to /admin/variants/[id]), `Заданий` (item._count?.variantTasks ?? item.variantTasks?.length ?? 0), `Статус` (statusBadge with togglePublish_published or togglePublish_draft), `Создан` (formatDate), `Действия` (Edit link + publish toggle button + inline rowError when applicable)
    - Publish toggle button text: `Опубликовать` when draft, `Снять с публикации` when published
    - Empty state in tbody when items.length === 0: `<tr><td colSpan={5} className={sharedStyles.empty}><div className={sharedStyles.emptyTitle}>Вариантов ещё нет</div><div className={sharedStyles.emptySub}>Создайте первый вариант и добавьте задания из банка.</div></td></tr>`
    - Pagination block when totalPages > 1, mirroring task-bank/page.tsx lines 230-242

    Apply `style={{ opacity: loading ? 0.6 : 1 }}` to the table. No filters needed for VARIANT-04 admin list (CONTEXT.md / D-12 specifies title, task count, published status, toggle — no filtering requirement).

    Tab indentation. Single quotes.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "app/admin/variants/(page|AdminVariants)" || echo "OK: list page compiles"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/app/admin/variants/AdminVariants.module.css` exists
    - `grep -c "\\.togglePublish\\s*{" frontend/src/app/admin/variants/AdminVariants.module.css` returns `1`
    - `grep -c "\\.togglePublish_published" frontend/src/app/admin/variants/AdminVariants.module.css` returns `1`
    - `grep -c "\\.togglePublish_draft" frontend/src/app/admin/variants/AdminVariants.module.css` returns `1`
    - `grep -c "\\.statusBadge" frontend/src/app/admin/variants/AdminVariants.module.css` returns `1`
    - `grep -c "background:\\s*#c4edc8\\|color:\\s*#15803d" frontend/src/app/admin/variants/AdminVariants.module.css` returns `2` (published colors from UI-SPEC)
    - File `frontend/src/app/admin/variants/page.tsx` exists
    - `grep -c "'use client'" frontend/src/app/admin/variants/page.tsx` returns `1`
    - `grep -c "import variantsService" frontend/src/app/admin/variants/page.tsx` returns `1`
    - `grep -c "import sharedStyles from '../task-bank/AdminTaskBank.module.css'" frontend/src/app/admin/variants/page.tsx` returns `1`
    - `grep -c "adminList\\|adminUpdate" frontend/src/app/admin/variants/page.tsx` returns at least `2`
    - `grep -c "+ Создать вариант" frontend/src/app/admin/variants/page.tsx` returns `1`
    - `grep -c "Вариантов ещё нет" frontend/src/app/admin/variants/page.tsx` returns `1`
    - `grep -c "Ошибка при публикации — попробуйте снова" frontend/src/app/admin/variants/page.tsx` returns `1`
    - `grep -c "Опубликован\\|Черновик" frontend/src/app/admin/variants/page.tsx` returns at least `2`
    - `cd frontend && npx tsc --noEmit` exits 0 with no errors mentioning admin/variants
  </acceptance_criteria>
  <done>Admin variants list renders with correct copy, publish toggle with optimistic update + error revert, pagination, empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Create VariantForm component + create-page wrapper</name>
  <files>
    frontend/src/app/admin/variants/VariantForm.tsx,
    frontend/src/app/admin/variants/VariantForm.module.css,
    frontend/src/app/admin/variants/new/page.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/task-bank/ExamTaskForm.tsx (analog component shape — useState pattern, handleSubmit, error banner, useRouter redirect)
    - frontend/src/app/admin/task-bank/ExamTaskForm.module.css (analog CSS — fieldset, input, label, error, footer, btn_save, btn_cancel, spinner classes)
    - frontend/src/app/admin/task-bank/new/page.tsx (analog server-component wrapper)
    - frontend/src/services/variants.service.ts (adminCreate signature)
    - .planning/phases/03-variant-composer/03-UI-SPEC.md §"Admin Variant Create Form" §"Copywriting Contract"
  </read_first>
  <action>
    **`frontend/src/app/admin/variants/VariantForm.module.css`** — create new file. Copy these classes from `frontend/src/app/admin/task-bank/ExamTaskForm.module.css` verbatim (tab indentation; do not modify any values):
    - `.form` (root flex column)
    - `.fieldset` (white card wrapper for input groups)
    - `.label` (form field label)
    - `.input` (text input style with focus border #6366f1)
    - `.textarea` (multiline input)
    - `.error` (red banner)
    - `.footer` (row containing save + cancel buttons)
    - `.btn_save` (primary dark button)
    - `.btn_cancel` (secondary link/button)
    - `.spinner` (inline 16px spinner)

    If `ExamTaskForm.module.css` has additional classes (format-specific stuff), DO NOT copy them — only the generic ones listed above.

    **`frontend/src/app/admin/variants/VariantForm.tsx`** — create new file. `'use client'` at top.

    Imports:
    ```
    import { useState } from 'react'
    import { useRouter } from 'next/navigation'
    import Link from 'next/link'
    import variantsService, { VariantListItem, CreateVariantPayload } from '@/services/variants.service'
    import styles from './VariantForm.module.css'
    ```

    Default-export `function VariantForm()` (no props — this is create-only; Plan 06's builder page handles edit-of-meta inline).

    State:
    - `form: { title: string; description: string }` (init `{ title: '', description: '' }`)
    - `submitting: boolean` (init false)
    - `error: string | null` (init null)

    Helpers:
    - `handleChange(field: 'title' | 'description', value: string)` — `setForm(prev => ({ ...prev, [field]: value }))`
    - `handleSubmit(e: React.FormEvent)` — `e.preventDefault()`. If `form.title.trim() === ''` set error to a Russian message and return. Set submitting true, error null. Try: `const dto: CreateVariantPayload = { title: form.title.trim(), description: form.description.trim() || undefined }; const result = await variantsService.adminCreate(dto); router.push('/admin/variants/' + result.id)`. Catch: read `(err as any).response?.data?.message` if present, else fall back to `'Не удалось создать вариант. Попробуйте ещё раз.'`. Finally: `setSubmitting(false)`.

    Render structure:
    - `<form className={styles.form} onSubmit={handleSubmit}>`
    - `<div className={styles.fieldset}>`
    - Title label + input (`type="text" required maxLength={200}`, value bound to form.title)
    - Description label + textarea (`maxLength={500} rows={4}`, value bound to form.description) — UI-SPEC says 0–500 chars
    - `</div>`
    - Error banner `{error && <div className={styles.error}>{error}</div>}`
    - Footer with save button (text `Создать вариант`, when submitting shows `<span className={styles.spinner} />` and disabled) and cancel `<Link href="/admin/variants" className={styles.btn_cancel}>Отмена</Link>` — per UI-SPEC copywriting `Отмена` (not `← Варианты` — that's the back-link copy on the page header, separate element).

    Russian copy lock:
    - Title field label: `Название варианта` (label) and placeholder/optional
    - Description field label: `Описание` (label), placeholder: `Необязательно`
    - Empty-title error: `Введите название варианта`
    - Generic submit error: `Не удалось создать вариант. Попробуйте ещё раз.`
    - Submit button: `Создать вариант`
    - Cancel button label: `Отмена`

    **`frontend/src/app/admin/variants/new/page.tsx`** — create new file. NO 'use client' (server component). Copy `frontend/src/app/admin/task-bank/new/page.tsx` shape verbatim, swap labels:

    ```
    import Link from 'next/link'
    import VariantForm from '../VariantForm'

    export default function NewVariantPage() {
      return (
        <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
            <Link href="/admin/variants" style={{ fontSize: 13, color: '#6b7280', textDecoration: 'none' }}>
              ← Варианты
            </Link>
            <h1 style={{ fontSize: 24, fontWeight: 700, color: '#111827', margin: 0 }}>
              Новый вариант
            </h1>
          </div>
          <VariantForm />
        </div>
      )
    }
    ```

    Use the exact inline styles from the task-bank/new analog — these are intentional one-offs to match the existing header pattern without spawning yet another CSS module.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "admin/variants/(VariantForm|new)" || echo "OK: form + new page compile"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/app/admin/variants/VariantForm.module.css` exists
    - `grep -c "\\.form\\s*{\\|\\.fieldset\\s*{\\|\\.input\\s*{\\|\\.error\\s*{\\|\\.btn_save\\s*{\\|\\.btn_cancel\\s*{\\|\\.spinner\\s*{" frontend/src/app/admin/variants/VariantForm.module.css` returns at least `7`
    - File `frontend/src/app/admin/variants/VariantForm.tsx` exists
    - `grep -c "'use client'" frontend/src/app/admin/variants/VariantForm.tsx` returns `1`
    - `grep -c "variantsService.adminCreate" frontend/src/app/admin/variants/VariantForm.tsx` returns `1`
    - `grep -c "router.push" frontend/src/app/admin/variants/VariantForm.tsx` returns `1`
    - `grep -c "Создать вариант" frontend/src/app/admin/variants/VariantForm.tsx` returns at least `1`
    - `grep -c "Не удалось создать вариант" frontend/src/app/admin/variants/VariantForm.tsx` returns `1`
    - `grep -c "Название варианта" frontend/src/app/admin/variants/VariantForm.tsx` returns `1`
    - `grep -c "maxLength={200}" frontend/src/app/admin/variants/VariantForm.tsx` returns `1`
    - `grep -c "maxLength={500}" frontend/src/app/admin/variants/VariantForm.tsx` returns `1`
    - File `frontend/src/app/admin/variants/new/page.tsx` exists
    - `grep -c "VariantForm" frontend/src/app/admin/variants/new/page.tsx` returns at least `2` (import + render)
    - `grep -c "← Варианты" frontend/src/app/admin/variants/new/page.tsx` returns `1`
    - `grep -c "Новый вариант" frontend/src/app/admin/variants/new/page.tsx` returns `1`
    - `cd frontend && npx tsc --noEmit` exits 0 with no errors mentioning admin/variants/
    - `cd frontend && npm run build` exits 0 (Next.js production build succeeds)
  </acceptance_criteria>
  <done>Create form renders title + description; submits to backend; redirects to builder; back-link works; type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→frontend SSR/CSR→backend | Frontend client component sends form fields through `api` wrapper (cookies + CORS already established). |
| user input→DOM | Title and description are uncontrolled-input → controlled-state. React escapes by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-22 | Tampering | XSS via malicious title injected through textarea/input | mitigate | React JSX escapes interpolated values by default; no `dangerouslySetInnerHTML` used. `grep -c "dangerouslySetInnerHTML"` in modified files returns 0. |
| T-03-23 | Tampering | Mass-assignment in create form sending extra fields | mitigate | Frontend builds the DTO from explicit fields only (`{ title, description }`); backend ValidationPipe whitelist strips unknowns. Even if frontend leaked extra fields, backend strips them. |
| T-03-24 | Information Disclosure | Optimistic publish toggle reveals row data to other tabs via localStorage | accept | No localStorage writes; state is component-local React useState only. |
| T-03-25 | Denial of Service | User spams publish toggle rapidly | mitigate | `toggleBusyId` state disables the button (`disabled={toggleBusyId === item.id}`) while a PATCH is in-flight. Verified by adding `disabled` attribute referencing `toggleBusyId`. |
| T-03-26 | Spoofing | Non-admin user navigates to /admin/variants directly | mitigate | Existing AdminLayout (frontend/src/app/admin/layout.tsx lines 25-32) redirects non-admins. No change needed. |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- All 5 new files exist at the declared paths
- `grep -r "Опубликован\\|Черновик" frontend/src/app/admin/variants/page.tsx` matches expected copy strings
- variantsService.adminList + adminUpdate + adminCreate are all called from the new pages
- Pagination block renders only when `totalPages > 1`
</verification>

<success_criteria>
- /admin/variants displays the list of variants with title, task count, status, created date, actions
- Publish toggle works optimistically; reverts on error with inline 3-sec message
- /admin/variants/new shows the create form; submits POST /admin/variants; redirects to /admin/variants/[id]
- Frontend type-checks and builds clean
- VARIANT-01 (create) and VARIANT-03 (publish/unpublish) satisfied at the UI layer
</success_criteria>

<output>
After completion, create `.planning/phases/03-variant-composer/03-05-SUMMARY.md` recording: files shipped, copy strings honored, key decisions (CSS reused via sharedStyles import to avoid 200 lines duplication; new togglePublish classes added per UI-SPEC; create page is server component wrapping client form per Phase 2 precedent). Note: Plan 06 (variant builder) and Plan 07 (student catalog) are in the same wave — they should be checked against this plan for any unintended file overlap (there should be none: this plan touches `page.tsx`, `new/page.tsx`, `VariantForm.*`; Plan 06 touches `[id]/page.tsx`, `[id]/VariantBuilder.module.css`; Plan 07 touches `frontend/src/app/variants/*` not admin/variants).
</output>
