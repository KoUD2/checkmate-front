---
phase: 03-variant-composer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/variants/variants.service.spec.ts
  - backend/src/variants/variants.controller.spec.ts
autonomous: true
requirements:
  - VARIANT-01
  - VARIANT-02
  - VARIANT-03
  - VARIANT-04
  - VARIANT-05
tags:
  - nestjs
  - jest
  - tests
  - variants
  - wave-0

must_haves:
  truths:
    - "Two spec files exist under backend/src/variants/ — one for service, one for controller"
    - "Each spec compiles standalone (imports do not yet exist; spec uses `it.todo()` placeholders)"
    - "Jest discovers both spec files via the default `backend/jest.config.js` testRegex"
    - "Running `cd backend && npx jest --testPathPattern variants --passWithNoTests` exits 0"
  artifacts:
    - path: "backend/src/variants/variants.service.spec.ts"
      provides: "Skeleton test file with it.todo() placeholders covering VARIANT-02, VARIANT-03, VARIANT-04, VARIANT-05"
      contains: "describe('VariantsService'"
    - path: "backend/src/variants/variants.controller.spec.ts"
      provides: "Skeleton test file with it.todo() placeholders covering VARIANT-01 (DTO) and VARIANT-03 (RolesGuard)"
      contains: "describe('VariantsController'"
  key_links:
    - from: "backend/src/variants/variants.service.spec.ts"
      to: "backend/jest.config.js testRegex"
      via: "file path matches `.spec.ts$`"
      pattern: "describe\\('VariantsService'"
    - from: "backend/src/variants/variants.controller.spec.ts"
      to: "backend/jest.config.js testRegex"
      via: "file path matches `.spec.ts$`"
      pattern: "describe\\('VariantsController'"
---

<objective>
Create Wave-0 Jest spec skeletons for VariantsService and VariantsController so downstream plans (02, 03) can fill in `it.todo()` placeholders with real assertions. The spec files exist before any service/controller source is written so Plan 02 and Plan 03 land in a "test-driven by file presence" state — Nyquist gate satisfied per phase research §Wave 0 Gaps.

Purpose: Establish failing-by-placeholder test scaffolds that match the analog `exam-tasks.service.spec.ts` / `exam-tasks.controller.spec.ts` structure. This is the established Phase 2 Wave-0 pattern (`02-00-PLAN.md`).
Output: Two `.spec.ts` files with `describe()` blocks and `it.todo()` placeholders. No source files yet — those land in Plan 02/03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-variant-composer/03-CONTEXT.md
@.planning/phases/03-variant-composer/03-RESEARCH.md
@.planning/phases/03-variant-composer/03-PATTERNS.md
@backend/src/exam-tasks/exam-tasks.service.spec.ts
@backend/src/exam-tasks/exam-tasks.controller.spec.ts
@backend/jest.config.js

<interfaces>
<!-- The variants service/controller do not yet exist. Spec files use forward-reference imports that compile because TypeScript resolves `from './variants.service'` only when those files land in Plan 02/03. To make Wave 0 land green WITHOUT Plan 02/03 sources, the spec files MUST use `it.todo()` (which never runs the import logic) and the `import { VariantsService } from './variants.service'` line must be commented out OR replaced with a `type-only` reference that doesn't break compilation. -->

Wave-0 pattern from `backend/src/exam-tasks/exam-tasks.service.spec.ts` (Phase 2 P00 analog): import the not-yet-created class, set up `prisma` mock, and use `it.todo()` for cases that will be implemented in Plan 02 / Plan 03.

The Phase 2 P00 SUMMARY confirms this approach is acceptable: skeleton specs were created with imports referencing types that landed in subsequent waves; Jest tolerated this because `it.todo()` blocks never execute. For Phase 3, to guarantee `npx jest --testPathPattern variants` exits 0 BEFORE Plan 02 creates the source files, write the spec files with the import lines commented out behind a `// TODO(plan-02)` marker. Plan 02 will uncomment them when adding real assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create variants.service.spec.ts skeleton</name>
  <files>backend/src/variants/variants.service.spec.ts</files>
  <read_first>
    - backend/src/exam-tasks/exam-tasks.service.spec.ts (analog Phase 2 spec structure — copy the prisma mock setup and describe block)
    - backend/jest.config.js (confirm testRegex picks up `*.spec.ts`)
    - .planning/phases/03-variant-composer/03-RESEARCH.md §"Validation Architecture" (test list)
  </read_first>
  <action>
    Create new file `backend/src/variants/variants.service.spec.ts` with a `describe('VariantsService', () => { ... })` block. Inside, declare `let prisma: any` and a `beforeEach` that initializes the mock with: `variant: { findUnique, findMany, count, create, update, delete }`, `variantTask: { findMany, deleteMany, createMany }`, `examTask: { findMany, findUnique }`, and `$transaction: jest.fn().mockImplementation((ops) => Promise.all(ops))`. DO NOT yet import `VariantsService` — instead place a single-line comment `// TODO(plan-02): import { VariantsService } from './variants.service'` at the top of the file. Then add five `it.todo()` placeholders verbatim:
    - `it.todo('assignTasks() executes $transaction([deleteMany, createMany]) with positions 1.0, 2.0, 3.0 from taskIds order (VARIANT-02)')`
    - `it.todo('assignTasks() throws BadRequestException when taskIds contains duplicates (VARIANT-02)')`
    - `it.todo('update() sets published=true and returns variant (VARIANT-03)')`
    - `it.todo('listPublished() builds where: { published: true } and returns items + total + totalPages (VARIANT-04)')`
    - `it.todo('getById() includes variantTasks orderBy position asc with examTask (VARIANT-05)')`
    Use tab indentation (consistent with `exam-tasks.service.spec.ts`). The file must compile with `tsc` and pass Jest discovery with zero failing/passing tests (only todo).
  </action>
  <verify>
    <automated>cd backend && npx jest --testPathPattern variants.service.spec --passWithNoTests 2>&1 | grep -E "Tests:.*5 todo|todo: 5"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `backend/src/variants/variants.service.spec.ts`
    - `grep -c "describe('VariantsService'" backend/src/variants/variants.service.spec.ts` returns `1`
    - `grep -c "it.todo(" backend/src/variants/variants.service.spec.ts` returns exactly `5`
    - `grep -c "TODO(plan-02): import { VariantsService }" backend/src/variants/variants.service.spec.ts` returns `1`
    - `grep -c "VARIANT-02\|VARIANT-03\|VARIANT-04\|VARIANT-05" backend/src/variants/variants.service.spec.ts` returns `5` (all four IDs appear; VARIANT-02 appears twice)
    - Running `cd backend && npx jest --testPathPattern variants.service.spec --passWithNoTests` exits with code 0
    - Jest summary line reports `5 todo` tests
  </acceptance_criteria>
  <done>variants.service.spec.ts exists, Jest discovers it, all 5 `it.todo()` placeholders are present with phase requirement IDs in the message strings.</done>
</task>

<task type="auto">
  <name>Task 2: Create variants.controller.spec.ts skeleton</name>
  <files>backend/src/variants/variants.controller.spec.ts</files>
  <read_first>
    - backend/src/exam-tasks/exam-tasks.controller.spec.ts (analog — copy `makeMockContext` + `makeGuardWithAdminRole` helpers verbatim, change DTO import)
    - backend/src/common/guards/roles.guard.ts (confirm Reflector + ROLES_KEY signature still matches)
    - backend/src/common/decorators/roles.decorator.ts (confirm ROLES_KEY export)
  </read_first>
  <action>
    Create new file `backend/src/variants/variants.controller.spec.ts` mirroring `exam-tasks.controller.spec.ts` structure. Copy the helper functions `makeMockContext(userRole: Role)` and `makeGuardWithAdminRole()` verbatim (same imports from `@nestjs/common`, `@nestjs/core`, `@prisma/client`, and `../common/guards/roles.guard` + `../common/decorators/roles.decorator`). DO NOT yet import `CreateVariantDto` from `./dto/create-variant.dto` — place comment `// TODO(plan-03): import { CreateVariantDto } from './dto/create-variant.dto'` at top. Add three `it.todo()` placeholders:
    - `it.todo('CreateVariantDto rejects missing title (VARIANT-01)')`
    - `it.todo('AssignTasksDto rejects non-UUID entries in taskIds array (VARIANT-02)')`
    - `it.todo('RolesGuard blocks USER role on @Roles(Role.ADMIN) variants routes (VARIANT-03)')`
    Use tab indentation. File must compile and Jest must discover it.
  </action>
  <verify>
    <automated>cd backend && npx jest --testPathPattern variants.controller.spec --passWithNoTests 2>&1 | grep -E "Tests:.*3 todo|todo: 3"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `backend/src/variants/variants.controller.spec.ts`
    - `grep -c "describe('VariantsController'" backend/src/variants/variants.controller.spec.ts` returns `1`
    - `grep -c "it.todo(" backend/src/variants/variants.controller.spec.ts` returns exactly `3`
    - `grep -c "function makeMockContext" backend/src/variants/variants.controller.spec.ts` returns `1`
    - `grep -c "function makeGuardWithAdminRole" backend/src/variants/variants.controller.spec.ts` returns `1`
    - `grep -c "TODO(plan-03): import { CreateVariantDto }" backend/src/variants/variants.controller.spec.ts` returns `1`
    - Running `cd backend && npx jest --testPathPattern variants.controller.spec --passWithNoTests` exits with code 0
    - Jest summary line reports `3 todo` tests
  </acceptance_criteria>
  <done>variants.controller.spec.ts exists, Jest discovers it, all 3 `it.todo()` placeholders are present, RolesGuard helpers identical to analog.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Jest discovery for both specs</name>
  <files>backend/src/variants/variants.service.spec.ts, backend/src/variants/variants.controller.spec.ts</files>
  <read_first>
    - backend/src/variants/variants.service.spec.ts (file just created)
    - backend/src/variants/variants.controller.spec.ts (file just created)
    - backend/jest.config.js (testRegex)
  </read_first>
  <action>
    Run `cd backend && npx jest --testPathPattern variants --passWithNoTests` and confirm Jest reports `8 todo` tests across two test suites (5 in service.spec, 3 in controller.spec), exit code 0, zero passing, zero failing. If the output does not match, inspect the spec files and fix syntax errors (mismatched braces, missing `describe` callback, wrong import paths). Do NOT add real assertions in this task — those land in Plan 02 / Plan 03. The Nyquist gate is "Jest discovers our specs" — that is the only requirement at Wave 0.
  </action>
  <verify>
    <automated>cd backend && npx jest --testPathPattern variants --passWithNoTests --silent 2>&1 | grep -E "Tests:.*8 todo|Test Suites:.*2 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npx jest --testPathPattern variants --passWithNoTests` exits with code 0
    - Jest summary contains `Test Suites: 2 passed, 2 total`
    - Jest summary contains `Tests:` line with `8 todo` count
    - No failing tests, no errored test suites in the output
  </acceptance_criteria>
  <done>Both spec files compile and Jest reports 8 todo tests across 2 suites with exit 0. Wave 0 gate cleared; Plan 02 and Plan 03 can proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→test-runner | Untrusted nothing — local Jest run, no network, no data. Wave-0 scaffolds are inert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Spec file with stray it() calls that fail Jest | mitigate | All assertions are `it.todo()` only; verified by `grep -c "it.todo("` returning exact count (5 / 3) |
| T-03-02 | Denial of Service | Spec file with infinite loop or unhandled async | accept | Wave-0 has no real assertions; impossible to hang. |
| T-03-03 | Information Disclosure | Spec file accidentally importing prod secrets | accept | No imports beyond `@nestjs/*`, `@prisma/client`, and project-local guards/decorators. No env reads. |
</threat_model>

<verification>
- Both spec files exist under `backend/src/variants/`
- `cd backend && npx jest --testPathPattern variants --passWithNoTests` exits 0 with `8 todo` total
- `grep -c "VARIANT-0" backend/src/variants/*.spec.ts | grep -v '^#'` confirms all 5 requirement IDs appear at least once across the two files
</verification>

<success_criteria>
- Two `.spec.ts` files exist with `describe()` blocks and `it.todo()` placeholders for all 5 VARIANT-* requirements
- Jest discovers and reports `8 todo` tests, 0 passing, 0 failing
- No source files (variants.service.ts, variants.controller.ts, etc.) exist yet — Plans 02 and 03 add those
</success_criteria>

<output>
After completion, create `.planning/phases/03-variant-composer/03-01-SUMMARY.md` recording: file paths created, Jest todo counts confirmed (5 + 3 = 8), Nyquist gate passed.
</output>
