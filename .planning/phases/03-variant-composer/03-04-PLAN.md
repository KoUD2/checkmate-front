---
phase: 03-variant-composer
plan: 04
type: execute
wave: 3
depends_on:
  - 03
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/services/variants.service.ts
  - frontend/src/app/admin/layout.tsx
autonomous: true
requirements:
  - VARIANT-01
  - VARIANT-02
  - VARIANT-03
  - VARIANT-04
  - VARIANT-05
tags:
  - nextjs
  - api-client
  - typescript
  - variants

must_haves:
  truths:
    - "frontend/package.json declares @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities as dependencies at versions 6.3.1 / 10.0.0 / 3.2.2"
    - "frontend/src/services/variants.service.ts exports a typed API client with all 8 backend routes: adminList, adminGetById, adminCreate, adminUpdate, adminAssignTasks, adminRemove, list, getById"
    - "Type contract is complete: Variant, VariantTask, VariantListItem, VariantListResult, CreateVariantPayload, UpdateVariantPayload, ExamSection, TaskFormat"
    - "Admin sidebar has a new 'Варианты' nav entry pointing to /admin/variants, placed immediately after 'Банк заданий'"
    - "Admin sidebar active-state matching uses startsWith() for /admin/variants sub-routes (consistent with all non-/admin items)"
    - "Frontend type-checks cleanly: npm run build in frontend dir exits 0"
  artifacts:
    - path: "frontend/package.json"
      provides: "Three new dependency entries for @dnd-kit packages with locked versions"
      contains: "@dnd-kit/core"
    - path: "frontend/src/services/variants.service.ts"
      provides: "variantsService default export with 8 typed methods"
      contains: "export default variantsService"
    - path: "frontend/src/app/admin/layout.tsx"
      provides: "NAV_ITEMS array with new { href: '/admin/variants', label: 'Варианты' } entry"
      contains: "/admin/variants"
  key_links:
    - from: "frontend/src/services/variants.service.ts"
      to: "frontend/src/shared/utils/api.ts"
      via: "import api from '@/shared/utils/api'"
      pattern: "from '@/shared/utils/api'"
    - from: "frontend/src/app/admin/layout.tsx NAV_ITEMS"
      to: "frontend/src/app/admin/variants/page.tsx (will land in Plan 05)"
      via: "Link href=/admin/variants"
      pattern: "/admin/variants"
---

<objective>
Lay the frontend foundation that Plans 05, 06, and 07 will consume in parallel: install the @dnd-kit packages (needed only by Plan 06 but installed here so the package.json change is sequenced before frontend builds in Wave 4), create the typed `variantsService` API client (consumed by all three Wave-4 plans), and add the admin sidebar nav entry (small but blocking — without it the admin pages from Plan 05/06 are unreachable through the UI).

Purpose: Single small plan that resolves all Wave-4 frontend prerequisites. Without this, Plans 05/06/07 would each have to handle install + service creation, creating file-conflict and merge ordering risks. By isolating prerequisites here, Wave 4 plans can run truly parallel.
Output: package.json updated, variantsService typed client created, admin sidebar nav extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-variant-composer/03-CONTEXT.md
@.planning/phases/03-variant-composer/03-RESEARCH.md
@.planning/phases/03-variant-composer/03-PATTERNS.md
@.planning/phases/03-variant-composer/03-03-PLAN.md
@frontend/package.json
@frontend/src/services/exam-tasks.service.ts
@frontend/src/services/resources.service.ts
@frontend/src/shared/utils/api.ts
@frontend/src/app/admin/layout.tsx

<interfaces>
From `frontend/src/services/exam-tasks.service.ts` (verbatim API client pattern):
- Imports `api from '@/shared/utils/api'` and uses `api.get/post/patch/put/delete`.
- Each typed method awaits the response and returns `res.data.data` (unwraps the backend `{ success, data }` envelope).
- Type exports placed at the top of the file BEFORE the service object (so other files can import them).
- Default-exports the service object.

Backend routes shipped in Plan 03 (these define the contract this client implements):
| Method | Path | Body | Returns |
|---|---|---|---|
| GET | /admin/variants?page&limit | — | { items, total, totalPages } |
| GET | /admin/variants/:id | — | Variant with variantTasks + examTask |
| POST | /admin/variants | { title, description?, published? } | Variant |
| PATCH | /admin/variants/:id | Partial<CreateVariantPayload> | Variant |
| PUT | /admin/variants/:id/tasks | { taskIds: string[] } | Variant |
| DELETE | /admin/variants/:id | — | { deleted: true } |
| GET | /variants?page&limit | — | { items, total, totalPages } (only published) |
| GET | /variants/:id | — | Variant with variantTasks + examTask |

NPM packages to install (versions verified in RESEARCH §"Standard Stack"):
- `@dnd-kit/core@6.3.1`
- `@dnd-kit/sortable@10.0.0`
- `@dnd-kit/utilities@3.2.2`

Section/format types already exported by `frontend/src/services/exam-tasks.service.ts` — DO NOT re-declare them. Either re-export them from variants.service.ts (`export type { ExamSection, TaskFormat } from './exam-tasks.service'`) or import-use them and let downstream files import from either service. Choose re-export — keeps consumers using a single import path.

Admin nav active matching from `frontend/src/app/admin/layout.tsx` lines 49-58 (current code): `(item.href === '/admin' ? pathname === item.href : pathname === item.href || pathname.startsWith(item.href + '/'))`. The new `/admin/variants` entry inherits this behavior — no per-item logic needed; just add to the array.

Phase 2 SUMMARY note: `examTasksService uses api from @/shared/utils/api (not direct axios) — consistent with resources.service.ts pattern`. Apply identically here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit packages</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - frontend/package.json (current dependency list — confirm @dnd-kit is not present and verify React/Next versions are 19/16)
    - .planning/phases/03-variant-composer/03-RESEARCH.md §"Standard Stack" §"Environment Availability"
  </read_first>
  <action>
    Run `cd frontend && npm install --save @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 @dnd-kit/utilities@3.2.2`. Do NOT use `^` or `~` — pin exact versions to match the verified npm registry snapshot in RESEARCH.md (version drift in dnd-kit between major versions has caused API breakage per RESEARCH §Pitfall 1). After install, run `cd frontend && npm ls @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities` to confirm install succeeded with no peer-dependency warnings flagged as ERR. Warnings prefixed `npm WARN` are tolerable; an `npm ERR!` line is not.

    If install fails with React 19 peer-dep ERR, do NOT fall back to `--force` or `--legacy-peer-deps` without first reading the error: RESEARCH confirms `@dnd-kit/core@6.3.1` peer dep is `>=16.8.0` which is compatible with React 19. If a genuine version mismatch occurs, halt and surface the discrepancy rather than masking it.
  </action>
  <verify>
    <automated>cd frontend && npm ls @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities 2>&1 | grep -E "@dnd-kit/(core|sortable|utilities)@" | wc -l | grep -E "^\\s*3\\s*$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"@dnd-kit/core"' frontend/package.json` shows `"@dnd-kit/core": "6.3.1"` (exact, no `^` or `~`)
    - `grep '"@dnd-kit/sortable"' frontend/package.json` shows `"@dnd-kit/sortable": "10.0.0"`
    - `grep '"@dnd-kit/utilities"' frontend/package.json` shows `"@dnd-kit/utilities": "3.2.2"`
    - `frontend/package-lock.json` exists and contains entries for all three packages
    - `cd frontend && npm ls @dnd-kit/core` exits 0 (no missing dependency)
    - `cd frontend && npm ls @dnd-kit/sortable` exits 0
    - `cd frontend && npm ls @dnd-kit/utilities` exits 0
    - `cd frontend && node -e "require('@dnd-kit/core'); require('@dnd-kit/sortable'); require('@dnd-kit/utilities'); console.log('OK')"` prints `OK` (modules resolve)
  </acceptance_criteria>
  <done>All three @dnd-kit packages installed at pinned versions; modules resolve at runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Create variantsService typed API client</name>
  <files>frontend/src/services/variants.service.ts</files>
  <read_first>
    - frontend/src/services/exam-tasks.service.ts (verbatim API client shape — imports, type exports above service, default export at bottom)
    - frontend/src/services/resources.service.ts (alternate analog — confirms api wrapper pattern)
    - frontend/src/shared/utils/api.ts (confirm api.get/post/patch/put/delete return shapes)
    - backend/src/variants/variants.controller.ts (just shipped — confirm route paths)
    - .planning/phases/03-variant-composer/03-PATTERNS.md §"frontend/src/services/variants.service.ts"
  </read_first>
  <behavior>
    Service-method contracts (these are exercised by Wave 4 plans; no test file in this task, but contracts must hold):
    - `adminList(page = 1, limit = 20)` → `GET /admin/variants?page=N&limit=N`, returns `Promise<VariantListResult>`
    - `adminGetById(id: string)` → `GET /admin/variants/:id`, returns `Promise<VariantListItem>` (with variantTasks)
    - `adminCreate(dto: CreateVariantPayload)` → `POST /admin/variants`, returns `Promise<VariantListItem>`
    - `adminUpdate(id, dto: Partial<CreateVariantPayload>)` → `PATCH /admin/variants/:id`, returns `Promise<VariantListItem>`
    - `adminAssignTasks(id, taskIds: string[])` → `PUT /admin/variants/:id/tasks`, returns `Promise<VariantListItem>`
    - `adminRemove(id)` → `DELETE /admin/variants/:id`, returns `Promise<void>`
    - `list(page = 1, limit = 20)` → `GET /variants?page=N&limit=N`, returns `Promise<VariantListResult>` (only published)
    - `getById(id)` → `GET /variants/:id`, returns `Promise<VariantListItem>`
  </behavior>
  <action>
    Create new file `frontend/src/services/variants.service.ts`. Tab indentation, single quotes.

    Imports at top:
    ```
    import api from '@/shared/utils/api'
    import type { ExamSection, TaskFormat } from './exam-tasks.service'
    ```
    Re-export the shared enums so callers can do `import { ExamSection } from '@/services/variants.service'`:
    ```
    export type { ExamSection, TaskFormat } from './exam-tasks.service'
    ```

    Type declarations (placed before the service object):
    - `VariantTaskExamTaskMeta` — `{ id: string; title: string; section: ExamSection; format: TaskFormat }`.
    - `VariantTask` — `{ id: string; variantId: string; examTaskId: string; position: number; examTask: VariantTaskExamTaskMeta }`.
    - `VariantListItem` — `{ id: string; title: string; description: string | null; published: boolean; createdAt: string; updatedAt: string; variantTasks: VariantTask[]; _count?: { variantTasks: number } }`.
    - `VariantListResult` — `{ items: VariantListItem[]; total: number; totalPages: number }`.
    - `CreateVariantPayload` — `{ title: string; description?: string; published?: boolean }`.
    - `UpdateVariantPayload` — alias for `Partial<CreateVariantPayload>`.

    Service object (default-exported at bottom, mirroring `exam-tasks.service.ts` lines 63-96):
    ```
    const variantsService = {
      async adminList(page = 1, limit = 20): Promise<VariantListResult> {
        const qs = new URLSearchParams({ page: String(page), limit: String(limit) })
        const res = await api.get<{ data: VariantListResult }>(`/admin/variants?${qs}`)
        return res.data.data
      },
      async adminGetById(id: string): Promise<VariantListItem> { ... GET /admin/variants/${id} ... },
      async adminCreate(dto: CreateVariantPayload): Promise<VariantListItem> { ... POST /admin/variants ... },
      async adminUpdate(id: string, dto: UpdateVariantPayload): Promise<VariantListItem> { ... PATCH /admin/variants/${id} ... },
      async adminAssignTasks(id: string, taskIds: string[]): Promise<VariantListItem> { ... PUT /admin/variants/${id}/tasks body: { taskIds } ... },
      async adminRemove(id: string): Promise<void> { await api.delete(`/admin/variants/${id}`) },
      async list(page = 1, limit = 20): Promise<VariantListResult> { ... GET /variants?... ... },
      async getById(id: string): Promise<VariantListItem> { ... GET /variants/${id} ... },
    }

    export default variantsService
    ```

    Every method body follows the `exam-tasks.service.ts` pattern: build URLSearchParams when query params exist; await `api.<verb>` with the response generic `<{ data: T }>`; return `res.data.data`.

    DO NOT add console.log, error handling try/catch, or retry logic in the service — those concerns belong to the caller (consistent with Phase 2 conventions in exam-tasks.service.ts).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "variants.service" || echo "OK: variants.service compiles"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/services/variants.service.ts` exists
    - `grep -c "import api from '@/shared/utils/api'" frontend/src/services/variants.service.ts` returns `1`
    - `grep -c "export interface VariantListItem\|export interface VariantTask\|export interface VariantListResult\|export interface CreateVariantPayload" frontend/src/services/variants.service.ts` returns at least `4`
    - `grep -c "async adminList\|async adminGetById\|async adminCreate\|async adminUpdate\|async adminAssignTasks\|async adminRemove\|async list\|async getById" frontend/src/services/variants.service.ts` returns at least `8`
    - `grep -c "'/admin/variants'\|/admin/variants/" frontend/src/services/variants.service.ts` returns at least `6`
    - `grep -c "'/variants?\|'/variants/" frontend/src/services/variants.service.ts` returns at least `2`
    - `grep -c "export default variantsService" frontend/src/services/variants.service.ts` returns `1`
    - `grep -c "console\\.\\|axios" frontend/src/services/variants.service.ts` returns `0` (no console statements, no direct axios — all via api wrapper)
    - `cd frontend && npx tsc --noEmit` exits 0 with no errors mentioning `variants.service`
  </acceptance_criteria>
  <done>Typed API client exists at the canonical path, exposes 8 methods covering all backend routes, type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add "Варианты" admin sidebar nav entry</name>
  <files>frontend/src/app/admin/layout.tsx</files>
  <read_first>
    - frontend/src/app/admin/layout.tsx (current NAV_ITEMS array — confirm position 5 is `Банк заданий`)
    - .planning/phases/03-variant-composer/03-UI-SPEC.md §"Admin Sidebar Navigation"
  </read_first>
  <action>
    Open `frontend/src/app/admin/layout.tsx`. Locate the `NAV_ITEMS` array (currently 7 items). Insert ONE new object literal `{ href: '/admin/variants', label: 'Варианты' }` immediately after the `{ href: '/admin/task-bank', label: 'Банк заданий' }` entry and before the `{ href: '/admin/resources', label: 'Полезное' }` entry. Preserve existing indentation and trailing-comma style.

    Do NOT modify any other code in this file — the active-class logic, sidebar toggle handlers, and admin role guard remain unchanged. The new entry inherits the standard `startsWith` active matching automatically (Phase 2 D-04 convention).

    Result: NAV_ITEMS array has 8 entries; `Варианты` sits at index 6 between `Банк заданий` and `Полезное`.
  </action>
  <verify>
    <automated>grep -A 1 "/admin/task-bank" frontend/src/app/admin/layout.tsx | grep -c "/admin/variants"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "{ href: '/admin/variants', label: 'Варианты' }" frontend/src/app/admin/layout.tsx` returns `1`
    - `grep -B 1 "/admin/variants" frontend/src/app/admin/layout.tsx | grep -c "/admin/task-bank"` returns `1` (variants entry comes immediately after task-bank entry)
    - `grep -A 1 "/admin/variants', label: 'Варианты'" frontend/src/app/admin/layout.tsx | grep -c "/admin/resources"` returns `1` (variants entry comes immediately before resources entry)
    - `grep -c "href:" frontend/src/app/admin/layout.tsx` returns `8` (was 7, now 8)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0 (Next.js production build succeeds — catches lint + type errors)
  </acceptance_criteria>
  <done>Admin sidebar shows "Варианты" between "Банк заданий" and "Полезное"; Next.js build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→npm registry | NPM packages downloaded over HTTPS; integrity checked by package-lock.json + npm's built-in tarball hash verification. |
| browser→frontend→nginx→backend | Frontend client calls follow existing axios interceptor pipeline (JWT cookie attached via `api` wrapper). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering | Compromised @dnd-kit package on npm registry | accept | Standard npm supply-chain risk. Packages pinned to exact versions (6.3.1 / 10.0.0 / 3.2.2). package-lock.json captures integrity hashes. CONCERNS.md (project-wide) tracks supply-chain audit as out-of-scope. |
| T-03-19 | Information Disclosure | variantsService logs sensitive data | mitigate | Action explicitly forbids console.log statements; verified by `grep -c "console\\." returns 0`. |
| T-03-20 | Spoofing | Frontend bypasses backend auth by calling endpoints directly | accept | Backend RolesGuard enforces. Frontend is best-effort UX; security boundary is at the API. Plan 03 verified guard behavior. |
| T-03-21 | Elevation of Privilege | Non-admin user navigates to /admin/variants via direct URL | mitigate | Existing `AdminLayout` guards: lines 25-32 redirect non-admin users to `/`. The new nav entry is invisible to non-admins because the whole admin layout is gated. |
</threat_model>

<verification>
- `cd frontend && npm ls @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities` exits 0
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm run build` exits 0 (full Next.js build)
- `grep -c "/admin/variants" frontend/src/app/admin/layout.tsx` returns at least `1`
- variantsService default-exports an object with exactly 8 typed methods
</verification>

<success_criteria>
- @dnd-kit packages installed at pinned versions; package-lock updated
- variantsService typed API client exists and exposes all 8 backend routes
- Admin sidebar shows "Варианты" nav entry
- Frontend type-checks and builds clean
- Wave 4 plans (05, 06, 07) can now import variantsService and use the existing admin layout without further wiring
</success_criteria>

<output>
After completion, create `.planning/phases/03-variant-composer/03-04-SUMMARY.md` recording: npm packages installed (with versions), variantsService API methods (8 total), admin sidebar entry added, frontend build status (green). Note that Wave 4 plans can now begin in parallel.
</output>
